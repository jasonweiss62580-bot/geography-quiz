import type { GeographicEntity } from './types';

// SCAFFOLD — populate when world countries quiz mode is implemented.
// svgId = ISO A3 code (e.g. "USA", "FRA")
export const WORLD_COUNTRIES: GeographicEntity[] = [];
